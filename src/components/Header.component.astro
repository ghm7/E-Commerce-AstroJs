---
const { title, elementType, content } = Astro.props;
---
<main class="pb-5 pt-5 container">
  <div class="text-center">
    <h1>{ title }</h1>
    <p>
      { content }
      {elementType !== undefined 
      ? <strong>{ elementType }</strong> 
      : '' }
    </p>
  </div>
</main>